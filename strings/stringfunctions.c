// there are the many functions in the string that usees the library of the #include<string.h>
//1] on is the strlen(stringname)
//2] 2nd one is the string copy strcpy(destination)
//3] 3rd one is the string campre = strcmp(str1,str2)// it give the number like  the negative number and the positive number and the zero number---if the two strings are the 
// are the same it give the 0 number 
// 4] 4th one is the sub string is present in the string or not if presen it will give the base adress of the sub string's base aadress from the string if not it will return
//the null if it is the null then we have to understand that we dont have that substring in that sub string broiiiii------
//5] 5th one is the 